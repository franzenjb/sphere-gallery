{
  "name": "sphere-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "Inside-of-a-sphere image gallery — Three.js + GSAP, phantom.land-style drag with eased inertia",
  "scripts": {
    "serve": "python3 -m http.server 4179",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
